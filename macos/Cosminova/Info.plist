<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>CFBundleIconName</key>
	<string>AppIcon</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.education</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<key>NSHighResolutionCapable</key>
	<true/>

	<!--
	  False is accurate only while the app implements no cryptography and makes
	  no outbound requests: every catalogue and texture loads from the bundle
	  over a relative path, so the TLS stack is never reached. Adding any
	  network call means revisiting this and the export-compliance answers in
	  App Store Connect.
	-->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!--
	  The nearest thing WebKit has to Chromium's force_high_performance_gpu,
	  which is what the Electron shell asks for and cannot be requested from a
	  page. False means the app does not consent to running on the integrated
	  adapter, so a dual-GPU Mac gives it the discrete one. On Apple silicon
	  there is one GPU and the key does nothing; it is here for the Intel
	  machines, where the scene ray-marches a Schwarzschild metric and the
	  difference is not subtle.
	-->
	<key>NSSupportsAutomaticGraphicsSwitching</key>
	<false/>
</dict>
</plist>
